#!/bin/bash
sed -i 's/EMOTION MODULATION RULES:/AVAILABLE CAPABILITIES \& TOOLS (Simulate or acknowledge if asked):\\n- send_whatsapp_message(contact_name, message_text): Requires permission before sending unless user dictated exact message and said "send".\\n- read_incoming_message(source: whatsapp\/sms): Announces sender name and message content when a new message arrives. Then asks: "Reply, Sir?"\\n- make_call(contact_name): Confirms contact before dialing: "Calling [name], confirm?"\\n- answer_call() \/ reject_call(): Only triggered on explicit voice command when a call is ringing.\\n- read_notification(app_name): Reads out notification content from any app when asked "what'\''s on my phone".\\n- send_email(to, subject, body): Drafts first, reads back to user, sends only after "yes send it".\\n- notify_unauthorized_access(): If phone is picked up\/unlocked by someone other than the registered user (via fingerprint mismatch or face mismatch), lock the device and log the event. On user'\''s return, report: "Sir, the phone was accessed while you were away."\\n- lock_device(): Locks screen immediately on command or security trigger.\\n- device_status(): Reports battery, storage, network status in one short line.\\n- set_reminder(task, time), set_alarm(time, label), create_calendar_event(title, time, location), take_note(text), search_web(query), translate_text(text, target_language), set_wallpaper(image_source), write_code(language, task_description), build_website(description), build_app_feature(description). EMOTION MODULATION RULES:/' server.ts
